name: Traceability check (reusable)

# Canonical requirements-traceability check, shared by every Rolliq solution.
#
# Caller pattern (the caller must trigger on `pull_request`):
#   jobs:
#     traceability:
#       uses: infra-commons/devops/.github/workflows/traceability-reusable.yml@<SHA> # pin to an immutable SHA
#       permissions:
#         contents: read
#         pull-requests: write
#       secrets: inherit          # passes ANTHROPIC_API_KEY
#
# The caller MUST grant the two permissions above. This workflow declaring them on its
# own job is necessary but not sufficient: a called workflow may never hold more
# permission than its caller, so a caller that grants nothing caps this at nothing and
# the run fails at startup — producing no check-run at all rather than a red one.
# Resulting check: `<caller-job-id> / traceability`. Advisory only — do NOT make
# it a required status check; it posts a PR comment and never fails the build on
# findings.

# Default all permissions to none — each job declares exactly what it needs.
permissions: {}

on:
  workflow_call:
    secrets:
      ANTHROPIC_API_KEY:
        required: false   # absent in Dependabot/fork contexts; the check skips itself

jobs:
  traceability:
    name: traceability
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Skip forks and Dependabot PRs — no secret access.
    if: >-
      ${{ github.event.pull_request.head.repo.full_name == github.repository
          && github.actor != 'dependabot[bot]' }}
    permissions:
      contents: read
      pull-requests: write
    steps:
      # Pinned 2026-09-03 to an immutable main ancestor (infra-commons/meta#1284 ruling: one kind of ref);
      # Dependabot is the only mover from here. Never @main.
      - uses: infra-commons/devops/.github/actions/traceability@14ddfdc05a69ac171278b4f3fdf97d468dd8b54a # pinned 2026-09-03, infra-commons/meta#1284
        with:
          api-key: ${{ secrets.ANTHROPIC_API_KEY }}
          github-token: ${{ github.token }}
          pr-number: ${{ github.event.pull_request.number }}
          repo: ${{ github.repository }}
